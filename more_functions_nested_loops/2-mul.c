#include "main.h"

/**
 * mul()- multiplies a and b, returns product
 *
 * @a: the digit in question
 * @b: the digit in question
 *
 * Return: the product of a and b
 */

int mul(int a, int b)
{
return (a * b);
}
